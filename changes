group helper modules into helpers directory
add general helpers module
add get_sess_val to helpers module
add assigns_for_quarter query to handler
add formatting to handler
add session support to app; changes include
	remove POST method from ManageAssignments handler
	grab quarter and year in MainAdmin handler; add them to session; pass to template
	add config dict to partners and pass to app
	add BaseHandler that implements sessions
	CustomHandler inherits from BaseHandler
	update admin_common and assignment_view templates to add session support
